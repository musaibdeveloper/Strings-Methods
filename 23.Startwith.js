// The startsWith() method returns true if a string begins with specified character(s). If not, it returns false.

let str = "5musaib is learing"

let check = str.startsWith("5");

console.log(check); //true. 

