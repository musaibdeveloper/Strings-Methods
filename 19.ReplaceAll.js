// The replaceAll() method returns a new string with all matches of a pattern replaced by a replacement.

let str = "Hello! Javascript is easy to learn and understand"

let rep = str.replaceAll( "a", "z");

console.log(rep);

// Hello! Jzvzscript is ezsy to lezrn znd understznd.

// In the above code the a letter is replaced with z. 

