// The split() method divides a string into a list of substrings and returns them as an array.

let str = "hello,my,name,is,syed";

let split = str.split(",");

console.log(split); // [ 'hello', 'my', 'name', 'is', 'syed' ]

// Here the string is splitted into sub strings by the space gap.
// We can give the parameters and spit it accordingly.